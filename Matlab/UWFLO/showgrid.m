close all
figure
axes1 = axes('YTickLabel','','XTickLabel','');
box(axes1,'on');
grid(axes1,'on');
hold(axes1,'all');

% Create plot
plot(y,x,'Marker','*','LineStyle','none','Color',[0 0 1]);

% figure
% plot(C1)
